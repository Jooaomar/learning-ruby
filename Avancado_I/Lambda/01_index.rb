# Lambdas são similares aos blocks, mas podem ser salvas em variáveis para
# serem reutilizadas.

# Um block é o parâmetro para o lambda
first_lambda = lambda {puts "my first lambda"}
first_lambda.call   # Chamando
first_lambda.call   # Chamando
first_lambda.call   # Chamando
